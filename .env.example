# API Configuration
# Base URL for API requests (leave empty to use same origin in production)
VITE_API_BASE_URL=https://golazo.runasp.net/api/v1

# WebSocket/Socket.IO URL (leave empty to use same as API base URL)
VITE_SOCKET_URL=https://golazo.runasp.net/api/v1
